[package]
name = "lemmaspec-plan"
version = "0.1.0"
edition = "2021"
description = "Deterministic Markdown plan projection for LemmaSpec"
license = "MIT"
repository = "https://github.com/MortenHusted/lemmaspec"
readme = "README.md"

[dependencies]
lemmaspec = { path = "../..", version = "0.4.0" }
sha2 = "0.10"

[package.metadata.dist]
dist = false
